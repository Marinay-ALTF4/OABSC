
 OABSC Project Notes (Team Documentation)

 Recent Updates 

- Authentication and registration flow improved.
- Registration now redirects to login with the message: "Login your account".
- Register and Add User validations aligned (name/email/password rules).
- Custom duplicate email message added: "This email is already taken."
- Admin Patients area updated:
	- Patient dashboard kept intact.
	- `View patient list` opens a working users list page.
	- Added user actions: add, edit, soft delete, restore.
	- Current logged-in admin is marked in the list.
- Soft delete was implemented for users (non-permanent delete).
- Postman testing routes and API endpoints were added (, `/api/register`, `/api/login`, `/api/users`).


